const {
  searchBooksTitle,
  searchAuthorsName,
  getAuthor,
  getBook,
  searchBooksBySubject,
} = require("./searchDB");

module.exports = {
  searchBooksTitle,
  searchAuthorsName,
  getAuthor,
  getBook,
  searchBooksBySubject,
};
